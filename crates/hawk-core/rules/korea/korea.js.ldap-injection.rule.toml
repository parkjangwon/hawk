id = "korea.js.ldap-injection"
name = "LDAP injection"
description = "입력데이터 검증 및 표현 - LDAP 삽입(가-10): 외부 입력을 검증 없이 LDAP 검색 필터에 연결하면 필터 우회·주입이 가능하다."
recommendation = "LDAP 필터를 파라미터화하고 특수문자를 이스케이프한다."
category = "ldap-injection"
severity = "high"
confidence = "medium"
languages = ["javascript", "typescript"]
cwe = "CWE-90"
owasp = "A03:2021"

[pattern]
regex = "\\.search\\s*\\([^)]*(req\\.|\\$\\{|\\+)"